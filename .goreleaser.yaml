version: 2

project_name: pafu

before:
  hooks:
    - go mod tidy

builds:
  - id: pafu
    binary: pafu
    env:
      - CGO_ENABLED=0
      - GOPRIVATE=github.com/taigrr/apple-silicon-accelerometer
    goos:
      - darwin
    goarch:
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

release:
  github:
    owner: taigrr
    name: spank
  name_template: "Pafu {{ .Version }}"
